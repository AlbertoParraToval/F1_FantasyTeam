import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'F1_FantasyTeam',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
